# onetool-bench internal tasks

mod demo 'demo/justfile'

default:
    @just --list --unsorted

# Lint bench source
lint:
    uv run ruff check src/

# Format bench source
fmt:
    uv run ruff format src/
