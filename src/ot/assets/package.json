{
  "private": true,
  "description": "Build dependencies for inject.js annotation script",
  "dependencies": {
    "optimal-select": "^4.0.1"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  },
  "scripts": {
    "build": "esbuild inject-src.js --bundle --minify --format=iife --outfile=inject.js --target=es2020"
  }
}
